<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Your Cart</title>
  <link rel="stylesheet" href="/css/cart.css" />
</head>
<body>
  <div class="container">
    <h1>Your Cart</h1>

    <% if (cart.totalQty === 0) { %>
      <p>Your cart is empty.</p>
    <% } else { %>
      <ul class="cart-list">
        <% for (let id in cart.items) { %>
          <li>
            <strong><%= cart.items[id].item.name %></strong> - 
            Quantity: <%= cart.items[id].quantity %> - 
            Price: <%= cart.items[id].item.price * cart.items[id].quantity %> TK
            <form action="/cart/remove/<%= id %>" method="POST" class="inline-form">
              <button type="submit">Remove</button>
            </form>
          </li>
        <% } %>
      </ul>
      <p class="total"><strong>Total:</strong> <%= cart.totalPrice %> TK</p>

      <form action="/order/place" method="POST">
        <button class="place-order-btn" type="submit">Place Order</button>
      </form>
    <% } %>
  </div>
</body>
</html>
